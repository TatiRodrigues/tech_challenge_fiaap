FROM node:20-slim

WORKDIR /app

COPY package*.json ./

# Instala dependências e força o binário SWC para Debian/glibc
RUN npm ci && npm install --no-save @next/swc-linux-x64-gnu 2>/dev/null || true

COPY . .

EXPOSE 3001

CMD ["npm", "run", "dev"]
