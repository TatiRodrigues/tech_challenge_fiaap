'use client';

/**
 * @deprecated Use Redux hooks instead
 * Este arquivo foi substituído por Redux + Redux Thunks
 * Importe de @/store/hooks ao invés:
 * 
 * import { useAppSelector } from '@/store/hooks';
 * const { user } = useAppSelector(state => state.auth);
 */

export {};
